_236‍.x([["default",()=>_236‍.o]]);
class Library {
  constructor(books = []){
    this.books = books;
  }

  bookCount() {
    return this.books.length;
  }

  addBook(newBook) {
    this.books.push(newBook);
  }

  addBooks(newBooks){
    newBooks.forEach(book => this.books.push(book));
  }

  printInventory(){
    for (const {title: title, author: author} of books) {
      console.log(`${title} by ${author}`)
    }
 
  }


};



_236‍.d(Library);